---
name: Build, Scan and Push Docker Image

on:
  push:
    branches:
      - main
    paths-ignore:
      - ".github/workflows/**"
      - "README.md"
      - "scripts/**"
      - "appspec.yml"

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run GitLeaks scan
        uses: zricethezav/gitleaks-action@v2

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/github-ecr-role
          aws-region: ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.registry }}
          REPO_NAME: ${{ vars.ECR_REPOSITORY_NAME }} 
        run: |
          docker build -t $ECR_REGISTRY/$REPO_NAME:${{ github.sha }} .

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh
          sudo mv ./bin/trivy /usr/local/bin/trivy
          trivy --version

      - name: Scan Docker image with Trivy
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.registry }}
          REPO_NAME: ${{ vars.ECR_REPOSITORY_NAME }}
        run: |
          trivy image \
            --scanners vuln \
            --severity HIGH,CRITICAL \
            --exit-code 1 \
            $ECR_REGISTRY/$REPO_NAME:${{ github.sha }}

      - name: Tag & Push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.registry }}
          REPO_NAME: ${{ vars.ECR_REPOSITORY_NAME }}
        run: |
          docker tag $ECR_REGISTRY/$REPO_NAME:${{ github.sha }} $ECR_REGISTRY/$REPO_NAME:latest
          docker push $ECR_REGISTRY/$REPO_NAME:${{ github.sha }}
          docker push $ECR_REGISTRY/$REPO_NAME:latest

